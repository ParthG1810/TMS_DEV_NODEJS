# ============================================================
# TMS (Table Management System) Environment Configuration
# ============================================================
# Copy this file to .env and fill in your actual values

# Environment
# Options: development, production
NODE_ENV=development

# Server Configuration
PORT=47847
DEV_API=http://localhost:47847
PRODUCTION_API=https://your-production-url.com

# Database Configuration
# MySQL database connection settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Mysql
DB_NAME=TmsDb_Dev
DB_PORT=3306

# JWT Configuration (Optional - for future authentication)
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRES_IN=3d

# Upload Configuration (Optional - defaults are set in code)
# MAX_FILE_SIZE=5242880  # 5MB in bytes
# UPLOAD_DIR=public/uploads/recipes

# Company Logo Configuration
# Default logo URL/path - leave empty if no default logo
DEFAULT_COMPANY_LOGO=

# Gmail OAuth Configuration
# Get these credentials from Google Cloud Console
# 1. Go to https://console.cloud.google.com/
# 2. Enable Gmail API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URI: http://localhost:47847/api/gmail/callback
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:47847/api/gmail/callback

# Next.js Public API URL (used for OAuth redirects)
NEXT_PUBLIC_API_URL=http://localhost:47847

# ============================================================
# SETUP INSTRUCTIONS
# ============================================================
# 1. Copy this file: cp .env.example .env
# 2. Update DB_PASSWORD with your MySQL password
# 3. Update DB_NAME if you want a different database name
# 4. Update JWT_SECRET with a strong random string for production
# 5. Create the database: mysql -u root -p -e "CREATE DATABASE tms_database CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
# 6. Import schema: mysql -u root -p tms_database < ../database/schema.sql
# 7. Start the server: npm run dev
